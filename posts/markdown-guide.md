---
title: 마크다운 문법 가이드
date: 2026-06-28
tags: [마크다운, 가이드]
description: 헤딩, 코드블록, 표, 인용문 등 자주 쓰는 마크다운 문법을 정리했습니다.
---

마크다운으로 글을 쓸 때 자주 사용하는 문법들을 정리합니다.

## 헤딩

```
# H1
## H2
### H3
```

## 텍스트 강조

**굵게**, *기울임*, ~~취소선~~

## 코드

인라인 코드는 `backtick`으로 감쌉니다.

코드 블록은 세 개의 백틱과 언어 이름을 씁니다:

```javascript
function greet(name) {
  return `Hello, ${name}!`;
}

console.log(greet('World'));
```

```python
def fibonacci(n):
    a, b = 0, 1
    for _ in range(n):
        yield a
        a, b = b, a + b

print(list(fibonacci(10)))
```

## 인용문

> 마크다운은 읽기 쉽고 쓰기 쉬운 텍스트 형식으로,
> HTML로 변환하기 위해 만들어졌습니다.
>
> — John Gruber

## 목록

순서 없는 목록:

- 항목 1
- 항목 2
  - 중첩 항목
  - 중첩 항목 2
- 항목 3

순서 있는 목록:

1. 첫 번째
2. 두 번째
3. 세 번째

## 표

| 문법 | 설명 | 예시 |
|------|------|------|
| `**text**` | 굵게 | **굵게** |
| `*text*` | 기울임 | *기울임* |
| `` `code` `` | 인라인 코드 | `code` |
| `[링크](url)` | 링크 | [링크](#) |

## 수평선

---

이상으로 기본 마크다운 문법 가이드를 마칩니다.
